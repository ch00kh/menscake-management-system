spring:
  config:
    import: "optional:file:.env[.properties]"
  application:
    name: api
  profiles:
    active: dev
  jackson:
    time-zone: Asia/Seoul
  jpa:
    hibernate:
      ddl-auto: validate

app:
  jwt:
    access-token-ttl: PT15M
    refresh-token-ttl: P14D
